FROM klaemo/couchdb:2.0.0

WORKDIR '/couch'

ARG HOST
ARG COUCH_ADMIN
ARG COUCH_ADMIN_PASSWORD

ENV HOST=${HOST}
ENV COUCH_ADMIN=${COUCH_ADMIN}
ENV COUCH_ADMIN_PASSWORD=${COUCH_ADMIN_PASSWORD}

ADD setup.sh setup.sh
RUN chmod a+x setup.sh
